includes:
    - phpstan-baseline.neon

parameters:
    level: 8
    bootstrapFiles:
        - tests/bootstrap.php
    ignoreErrors:
        - '#Trait Crustum\\PluginManifest\\Manifest\\ManifestTrait is used zero times#'

services:
